-----------------------Instructions-----------------------
1>Place all files in a single folder as it is currently.
2> Do not move any file somewhere else or delete or rename it(or you have to reallocate it inside the source code).
3>Authentication key(for the updater person)="123"
    (this can be chaned inside program, so make the source code and this README FILE very private and only provide the executable file to clcints).
4>to stop writing the file(at time of updating) press CTRL + C.But This will also terminate the program as well so do not panic.